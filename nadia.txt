es para tarea_git_sistemas.bash
 #!/bin/bash

 #Agregar todos los archivos al commit

  git add .

 #Realizar commit con mensaje

  git commit -m "Este es un commit automático"

 #Pushear del commit al repositorio remoto

  git push origin master

 #mostrar status

  git status

 


 es para actividad_guiada.bash
#!/bin/bash

git remote add origin git@github.com:SebastianPi200247/app-chuck.git

if [ $? -eq 0 ];then
     echo "El repositorio remoto se configuro con exito"
     git remote -v

else
     echo $?
     git remote -v
fi


